# Server runs on port 8090 as required by the sample curl request
server.port=8090

# Enable DEBUG logging so the start/end logs in AuthenticationController are visible
logging.level.com.cognizant.springlearn.controller=DEBUG
